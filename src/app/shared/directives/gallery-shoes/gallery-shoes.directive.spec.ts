import { GalleryShoesDirective } from './gallery-shoes.directive';

describe('GalleryShoesDirective', () => {
  it('should create an instance', () => {
    const directive = new GalleryShoesDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
