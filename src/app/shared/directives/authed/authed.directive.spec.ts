import { AuthedDirective } from './authed.directive';

describe('AuthedDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthedDirective();
    expect(directive).toBeTruthy();
  });
});
