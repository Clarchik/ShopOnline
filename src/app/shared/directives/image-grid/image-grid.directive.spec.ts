import { ImageGridDirective } from './image-grid.directive';

describe('ImageGridDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageGridDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
