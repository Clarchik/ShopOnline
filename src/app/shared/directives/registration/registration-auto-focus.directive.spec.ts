import { RegistrationAutoFocusDirective } from './registration-auto-focus.directive';

describe('RegistrationAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new RegistrationAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
