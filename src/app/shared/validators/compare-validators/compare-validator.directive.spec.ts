import { CopmareValidatorDirective } from './compare-validator.directive';

describe('CopmareValidatorDirective', () => {
    it('should create an instance', () => {
        const directive = new CopmareValidatorDirective();
        expect(directive).toBeTruthy();
    });
});
